fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.9.2
PyMuPDF==1.24.10
sentence-transformers==3.0.1
chromadb==0.5.5
numpy==1.26.4
langchain>=0.1.7,<0.2
# use hub version compatible with transformers; do not pin huggingface_hub here
transformers==4.57.1
tokenizers==0.22.1
# Remove explicit langchain-core; langchain brings correct version
torch>=2.2.0; platform_system != 'Darwin' or platform_machine != 'arm64'
# On Apple Silicon, install torch separately if needed:
# pip install torch --index-url https://download.pytorch.org/whl/cpu
google-generativeai==0.7.2
python-multipart>=0.0.6
